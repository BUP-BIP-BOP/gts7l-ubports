# Пусто намеренно.
#
# На стоковой прошивке `lsmod` не показывает ни одного модуля — Samsung
# собирает драйверы внутрь ядра. Единственное исключение — Wi-Fi: модуль
# приезжает из вендорного раздела и грузится не отсюда, а из device-hacks,
# после того как поднимется контейнер и появится /dev/wlan.
