# Kernel modules that Android's init loads via modprobe lists and that systemd
# must load instead. GENERATE THIS FILE FROM YOUR DEVICE — do not guess:
#
#   tools/gen-modules-load.sh          (run with the tablet in TWRP or stock)
#
# Known-needed on this family (kept as a seed, the generator will overwrite):
snd-soc-cs35l41-i2c   # 4x Cirrus speaker amps
snd-soc-cirrus-amp
cnss2                 # QCA6390 wifi/bt
cnss_nl
cnss_utils
cnss_prealloc
wlan_firmware_service_v01
