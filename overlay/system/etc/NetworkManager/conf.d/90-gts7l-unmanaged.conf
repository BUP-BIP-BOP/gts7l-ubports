# The Wi-Fi chip exposes more than one interface: swlan0 for the softAP path and
# p2p0 for Wi-Fi Direct. Left managed, NetworkManager competes with itself over
# them and can attach the default route to the wrong one.
[keyfile]
unmanaged-devices=interface-name:swlan0;interface-name:p2p0
